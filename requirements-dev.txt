# Dependências mínimas para rodar a suíte de testes no CI.
# (O requirements.txt completo inclui Jupyter/Streamlit e pacotes Windows-only,
#  desnecessários e incompatíveis com o runner Linux do CI.)
numpy==2.4.6
pandas==3.0.3
scikit-learn==1.8.0
joblib==1.5.3
